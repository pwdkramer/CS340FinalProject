<!-- students_classes.hbs -->
<h1>Los Santos School District</h1>
<h2>Students_Classes</h2>

<a href="/">Home</a> <a href="/schools.hbs">Schools</a> <a href="/teachers.hbs">Teachers</a> <a href="/students.hbs">Students</a> <a href="/classes.hbs">Classes</a> <a href="/students_classes.hbs">Students_Classes</a>
<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>

<h2>Search By Class</h2>
<p>Search by class name using the field below. Partial class names are OK too!</p>
<form id="search-student_class-html" method='GET' action='/students_classes.hbs'>
    <label for="input-class_name">Search by Class Name: </label>
    <input type="text" name="class_name" id="input-class_name-html-search">
    
    <input type="submit">
    <a href='/students_classes.hbs'><button>Reset</button></a>
</form>

<h2>Search By Student</h2>
<p>Search by student's last name using the field below. Partial last names are OK too!</p>
<form id="search-student_class-html" method='GET' action='/students_classes.hbs'>
    <label for="input-last_name">Search by Last Name: </label>
    <input type="text" name="last_name" id="input-last_name-html-search">
    
    <input type="submit">
    <a href='/students_classes.hbs'><button>Reset</button></a>
</form>

<br><br><br>

{{!-- Create a table --}}
<table id="students_classes-table">

    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
            <th>delete</th>
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the class_id and student_id, in order --}}
        {{#each data}}
        <tr data-value=({{this.class_id}}, {{this.student_id}})>
            <td>{{this.class_id}}</td>
            <td>{{this.student_id}}</td>
            <td><button onclick="deleteStudentClass({{this.class_id}}, {{this.student_id}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>
<script src="./js/delete_student_class.js"></script>

{{!-- Form to add a record to the table  --}}
<h2>Adding Data with AJAX</h2>
<p>To add a new student-class relationship, please enter the information below and click 'Submit'!</p>
<form id="add-student_class-form-ajax">

    <select name="input-class_id" id="input-class_id">
        <option value="">Select a Class</option>
        {{#each classes}}
        <option value="{{this.class_id}}">{{this.class_name}} {{this.class_section}}</option>
        {{/each}}
    </select>

    <select name="input-student_id" id="input-student_id">
        <option value="">Select a Student</option>
        {{#each students}}
        <option value="{{this.student_id}}">{{this.first_name}} {{this.last_name}}</option>
        {{/each}}
    </select>

    <input type="submit">
</form>

{{!-- Embed our javascript to handle the DOM manipulation and AJAX request --}}
<script src="./js/add_student_class.js"></script>